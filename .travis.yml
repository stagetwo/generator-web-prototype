language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-web-prototype' ]; then cd .. && eval "mv $currentfolder generator-web-prototype" && cd generator-web-prototype; fi

